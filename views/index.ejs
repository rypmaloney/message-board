<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to the <%= title %></p>
      <div class="message-board">
        <% for(let i=0; i<messages.length; i++) { %>
            <div class="message">
                <p><%= messages[i].text %></p>
                <p>-<%=messages[i].user %> at <% messages[i].added %></p>
            </div>
        <% } %>
      </div>
  </body>
</html>
